using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pleyerController : MonoBehaviour
{
    public bool teleportacja;
    // Start is called before the first frame update
    void Start()
    {
        teleportacja = false;   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
